name: Build

on:
  push:

jobs:
  components:
    uses: ./.github/workflows/target_build_types.yaml
    with:
      target: 'components'
  context:
    uses: ./.github/workflows/target_build_types.yaml
    with:
      target: 'context'