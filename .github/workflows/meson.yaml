on:
  push:

jobs:
  components:
    uses: ./.github/workflows/target_build_types.yaml
    with:
      target: 'components'

  context:
    uses: ./actions/build/action.yaml
    with:
      target: 'context'

  coro:
    uses: ./actions/build/action.yaml
    with:
      target: 'coro'

  fiber:
    uses: ./actions/build/action.yaml
    with:
      target: 'fiber'

  executor:
    uses: ./actions/build/action.yaml
    with:
      target: 'executor'