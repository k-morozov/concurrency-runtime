project('go-runtime', 'cpp',
        version : '1.0.0',
        default_options : ['warning_level=3', 'cpp_std=c++20'])

#benchmark = subproject('benchmark')

subdir('src')
subdir('example')
subdir('test')
subdir('bench')